package cn.appmedia.adshelf;

import android.content.Context;
import android.view.View;

/**
 * Stub class for missing ShelfView SDK
 * This SDK is no longer available, so we create a stub to allow compilation
 */
public class ShelfView extends View {
    public ShelfView(Context context) {
        super(context);
    }
}
