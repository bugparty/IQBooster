package ccn.appmedia.adshelf.download;

import android.app.Service;
import android.content.Intent;
import android.os.IBinder;

/**
 * Stub class for missing DownLoadService
 * This SDK is no longer available, so we create a stub to allow compilation
 */
public class DownLoadService extends Service {
    @Override
    public IBinder onBind(Intent intent) {
        return null;
    }
}
